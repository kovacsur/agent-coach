# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Claude Code — local/session settings (plugin manifest stays at .claude-plugin/plugin.json)
.claude/
CLAUDE.local.md

# Cursor — commit .cursor/rules/ only; ignore other local IDE config
.cursor/*
!.cursor/rules/
!.cursor/rules/**

# Secrets and env files
.env
.env.*
!.env.example

# Scratch / working notes
docs/.temporary-research/
*.local.json
*.local.md
